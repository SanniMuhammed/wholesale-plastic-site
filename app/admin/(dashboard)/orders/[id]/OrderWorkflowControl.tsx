"use client";
import {useState} from "react"; import {useRouter} from "next/navigation"; import type {Order,PaymentStatus,DeliveryStatus} from "@/lib/cms/types"; import {updateOrderWorkflowAction} from "../actions";
export function OrderWorkflowControl({order}:{order:Order}){const r=useRouter();const[saving,setSaving]=useState(false);const[form,setForm]=useState({quote_amount:order.quote_amount?.toString()||"",quote_currency:order.quote_currency||"NGN",payment_status:order.payment_status||"pending",delivery_status:order.delivery_status||"pending",assigned_to:order.assigned_to||"",internal_note:order.internal_note||"",payment_note:order.payment_note||"",delivery_note:order.delivery_note||""});async function save(){setSaving(true);try{await updateOrderWorkflowAction(order.id,{quote_amount:form.quote_amount?Number(form.quote_amount):null,quote_currency:form.quote_currency,payment_status:form.payment_status as PaymentStatus,delivery_status:form.delivery_status as DeliveryStatus,assigned_to:form.assigned_to||null,internal_note:form.internal_note||null,payment_note:form.payment_note||null,delivery_note:form.delivery_note||null});r.refresh()}finally{setSaving(false)}}const field=(k:keyof typeof form)=>(e:any)=>setForm(f=>({...f,[k]:e.target.value}));return <div className="grid gap-4 rounded-lg border border-border bg-surface p-4 sm:p-5"><div><h2 className="text-sm font-semibold text-ink">Sales & delivery tracking</h2><p className="mt-1 text-xs text-muted">Keep the negotiated quote, payment and delivery progress in one place.</p></div><div className="grid gap-4 sm:grid-cols-3"><label className="grid gap-1 text-sm font-medium text-ink-soft">Quote amount<input inputMode="decimal" value={form.quote_amount} onChange={field("quote_amount")} className="rounded border border-border px-3 py-2.5 text-ink" placeholder="e.g. 250000"/></label><label className="grid gap-1 text-sm font-medium text-ink-soft">Currency<select value={form.quote_currency} onChange={field("quote_currency")} className="rounded border border-border px-3 py-2.5 text-ink"><option>NGN</option><option>USD</option><option>EUR</option><option>GBP</option></select></label><label className="grid gap-1 text-sm font-medium text-ink-soft">Assigned to<input value={form.assigned_to} onChange={field("assigned_to")} className="rounded border border-border px-3 py-2.5 text-ink" placeholder="Salesperson"/></label></div><div className="grid gap-4 sm:grid-cols-2"><label className="grid gap-1 text-sm font-medium text-ink-soft">Payment status<select value={form.payment_status} onChange={field("payment_status")} className="rounded border border-border px-3 py-2.5 text-ink"><option value="pending">Pending</option><option value="partial">Partially paid</option><option value="paid">Paid</option><option value="refunded">Refunded</option></select></label><label className="grid gap-1 text-sm font-medium text-ink-soft">Delivery status<select value={form.delivery_status} onChange={field("delivery_status")} className="rounded border border-border px-3 py-2.5 text-ink"><option value="pending">Pending</option><option value="preparing">Preparing</option><option value="dispatched">Dispatched</option><option value="delivered">Delivered</option><option value="not_required">Not required</option></select></label></div><div className="grid gap-4 sm:grid-cols-3"><label className="grid gap-1 text-sm font-medium text-ink-soft">Internal note<textarea rows={4} value={form.internal_note} onChange={field("internal_note")} className="rounded border border-border px-3 py-2.5 text-ink"/></label><label className="grid gap-1 text-sm font-medium text-ink-soft">Payment note<textarea rows={4} value={form.payment_note} onChange={field("payment_note")} className="rounded border border-border px-3 py-2.5 text-ink"/></label><label className="grid gap-1 text-sm font-medium text-ink-soft">Delivery note<textarea rows={4} value={form.delivery_note} onChange={field("delivery_note")} className="rounded border border-border px-3 py-2.5 text-ink"/></label></div><button type="button" onClick={save} disabled={saving} className="justify-self-end rounded bg-brand px-5 py-2.5 text-sm font-semibold text-white disabled:opacity-60">{saving?"Saving…":"Save tracking"}</button></div>}
