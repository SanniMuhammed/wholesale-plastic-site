import { listHomepageSections } from "@/lib/cms/settings"; import {listCategories} from "@/lib/cms/categories"; import {PageHeader} from "@/components/admin/AdminUI"; import {HomepageSectionManager} from "./HomepageSectionManager"; import {ExploreCategoriesManager} from "./ExploreCategoriesManager"; import {StructuredHomepageContent} from "@/components/admin/StructuredHomepageContent";
export const dynamic="force-dynamic";
const SECTION_LABELS:Record<string,string>={hero:"Hero",trust_bar:"Trust bar",how_it_works:"How it works",start_business:"Start a business",travel:"You don't need to travel",delivery_teaser:"Delivery teaser",final_cta:"Final call to action"};
export default async function HomepageContentPage(){const[sections,categories]=await Promise.all([listHomepageSections(),listCategories()]);const finalCta=sections.filter(s=>s.key==="final_cta");const other=sections.filter(s=>s.key!=="final_cta");return <div><PageHeader title="Homepage" description="Control homepage sections, images, category photos, the customer journey and trust statements."/><StructuredHomepageContent sections={sections}/><HomepageSectionManager sections={other} labels={SECTION_LABELS}/><ExploreCategoriesManager initialCategories={categories}/><HomepageSectionManager sections={finalCta} labels={SECTION_LABELS}/></div>}
