/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    // TODO: once real product photography is hosted somewhere,
    // add that host here, e.g.:
    // remotePatterns: [{ protocol: 'https', hostname: 'cdn.example.com' }]
  },
};

export default nextConfig;
